const Header: React.FC = () => {
  return (
    <header className="fixed top-0 left-0 w-full text-center text-lg py-4 bg-slate-200 bg-opacity-30 backdrop-blur-md z-50">
      <h1 className="font-semibold">Asteri Cloud</h1>
    </header>
  );
};

export default Header;